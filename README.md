# ciad-infra

# Init infra
When cloning this repo you need to clone and update submodules with the following command
 ```sh
    git submodule init
    git submodule update
 ```

 or using the sh script
 `./init.sh`